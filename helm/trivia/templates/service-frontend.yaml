apiVersion: v1
kind: Service
metadata:
  name: {{ include "trivia.frontend.fullname" . }}
  labels:
    {{- include "trivia.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trivia.frontend.selectorLabels" . | nindent 4 }}
